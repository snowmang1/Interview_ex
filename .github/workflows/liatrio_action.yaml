name: Liatrio_apprintice_tests
on: [push]

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - 
        name: checkout
        uses: actions/checkout@v3
      - 
        name: run tests
        uses: liatrio/github-actions/apprentice-action
