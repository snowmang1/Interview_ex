version: "3"
services:
  web:
    build: .
    working_dir: /usr/src/app
    ports:
      - "80:80" # set visible port
    networks:
      app_net:
        link_local_ips:
          - 172.17.0.1

networks:
  app_net:
      driver: default
